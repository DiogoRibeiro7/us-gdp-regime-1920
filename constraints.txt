# Pinned versions for a fully reproducible environment.
# Install with:  pip install -e . -c constraints.txt
# The pyproject.toml declares minimum-compatible ranges; this file pins the exact
# versions used to produce the committed report and figures.
arch==8.0.0
matplotlib==3.11.0
numpy==2.4.6
openpyxl==3.1.5
pandas==2.3.3
PyYAML==6.0.3
requests==2.34.2
scikit-learn==1.9.0
scipy==1.17.1
statsmodels==0.14.6
typer==0.27.0
